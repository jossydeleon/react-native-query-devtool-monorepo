const Placeholder = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
      }}
    >
      <h4 style={{ color: "white", textAlign: "center" }}>
        Waiting for a Query data
      </h4>
    </div>
  );
};

export default Placeholder;
