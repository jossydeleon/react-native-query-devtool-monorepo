import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  inputContainer: {
    borderRadius: 5,
    margin: 10,
    borderWidth: 1,
    padding: 5,
    backgroundColor: 'white',
  },
});
